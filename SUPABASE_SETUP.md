# 📋 Инструкция по настройке базы данных Supabase

## Порядок выполнения SQL файлов

Выполните SQL файлы в **SQL Editor** Supabase в следующем порядке:

### 1️⃣ Таблица голосов
**Файл:** `sql/votes.sql`
```sql
-- Создает таблицу marketplace_votes
-- Хранит голоса пользователей за идеи товаров
```
**Результат:** Таблица `marketplace_votes` с уникальным ключом (user_id, request_id)

---

### 2️⃣ Таблица платежей
**Файл:** `sql/payments.sql`
```sql
-- Создает таблицу marketplace_payments
-- Хранит платежи через Telegram Stars
```
**Результат:** Таблица `marketplace_payments` с constraints для проверки stars > 0

---

### 3️⃣ Таблица AI диалогов
**Файл:** `sql/conversations.sql`
```sql
-- Создает таблицу marketplace_conversations
-- Хранит все сообщения пользователей и ответы AI
```
**Результат:** Таблица `marketplace_conversations` для аналитики

---

### 4️⃣ Таблица запросов (идей)
**Файл:** `sql/requests.sql`
```sql
-- Создает таблицу marketplace_requests
-- Основная таблица с идеями товаров для маркетплейсов
```
**Результат:** Таблица `marketplace_requests` с полями votes_count, has_priority

---

### 5️⃣ Таблица системных сообщений
**Файл:** `sql/system-messages.sql`
```sql
-- Создает таблицу marketplace_system_messages
-- Хранит ID сообщений с топ-10 для редактирования
```
**Результат:** Таблица `marketplace_system_messages` с начальной записью для топ-10

---

## 🎯 Быстрая проверка после выполнения

Выполните в SQL Editor:

```sql
-- Проверка созданных таблиц
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name LIKE 'marketplace_%';

-- Должно вывести 5 таблиц:
-- marketplace_votes
-- marketplace_payments
-- marketplace_conversations
-- marketplace_requests
-- marketplace_system_messages
```

---

## ⚠️ Важные примечания

1. **Префикс `marketplace_`** — все таблицы второго бота имеют этот префикс для разделения данных с первым ботом (vf-telegram-bot)
2. **Та же БД** — используется тот же Supabase проект, что и у первого бота
3. **Безопасность** — все SQL файлы используют `IF NOT EXISTS` для безопасного повторного запуска
4. **Constraints** — payments.sql использует DO блоки для предотвращения дублирования ограничений

---

## ✅ Следующий шаг после настройки БД

После выполнения всех SQL миграций можно запустить бота:

```bash
npm start
```

Бот будет:
- Слушать сообщения в Telegram
- Проводить Customer Development через OpenAI
- Публиковать в канал @aidevelopersGG
- Обновлять топ-10 каждый день в 12:00 МСК
